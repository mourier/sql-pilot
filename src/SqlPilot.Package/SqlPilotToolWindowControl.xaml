<UserControl x:Class="SqlPilot.Package.SqlPilotToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:SqlPilot.UI.Controls;assembly=SqlPilot.UI"
             Focusable="True">

  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Update notification bar (hidden by default) -->
    <Border x:Name="UpdateBar"
            Grid.Row="0"
            Background="#0078D4"
            Padding="8,4"
            Visibility="Collapsed">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="UpdateText"
                   Grid.Column="0"
                   Foreground="White"
                   VerticalAlignment="Center"
                   Text="A new version of SQL Pilot is available." />
        <Button x:Name="DownloadButton"
                Grid.Column="1"
                Content="Download"
                Margin="8,0"
                Padding="12,2"
                Click="DownloadButton_Click" />
        <Button x:Name="DismissButton"
                Grid.Column="2"
                Content="X"
                Padding="6,2"
                Click="DismissButton_Click" />
      </Grid>
    </Border>

    <!-- Refresh / status bar -->
    <Border Grid.Row="1" Padding="4,2" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="IndexStatus" Grid.Column="0" FontSize="11"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   VerticalAlignment="Center" Text="Not indexed. Connect to a server to start." />
        <Button x:Name="RefreshButton" Grid.Column="1" Content="Refresh" Padding="8,2"
                FontSize="11" Click="RefreshButton_Click" />
      </Grid>
    </Border>

    <!-- Main search control -->
    <controls:SearchControl x:Name="SearchPanel" Grid.Row="2" />
  </Grid>
</UserControl>
